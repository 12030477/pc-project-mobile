/// PC Build Budget Helper - Flutter Mobile Application
///
/// Entry point that wires up the top-level app widget.
library pc_build_budget_helper;

import 'package:flutter/material.dart';

import 'app/app.dart';

void main() {
  runApp(const PCBuildBudgetApp());
}
